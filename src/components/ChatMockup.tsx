import { Bot, Check, CheckCheck } from "lucide-react";
import { cn } from "@/lib/utils";
type Msg={from:"bot"|"user";text:string;time:string};
export function ChatMockup({title="Telegram bot",subtitle="onlayn · 24/7 javob beradi",messages,className,footerNote}:{title?:string;subtitle?:string;messages:Msg[];className?:string;footerNote?:string}){return <div className={cn("overflow-hidden rounded-2xl border border-border bg-card shadow-lift",className)}><div className="flex items-center gap-3 border-b border-border bg-navy px-4 py-3"><div className="grid size-9 shrink-0 place-items-center rounded-full bg-primary/20 text-primary-foreground"><Bot className="size-5"/></div><div><p className="text-sm font-semibold text-navy-foreground">{title}</p><p className="text-xs text-navy-foreground/60">{subtitle}</p></div></div><div className="space-y-3 bg-sand/60 px-4 py-5">{messages.map((m,i)=><div key={i} className={cn("flex",m.from==="user"?"justify-end":"justify-start")}><div className={cn("max-w-[85%] rounded-xl border px-3.5 py-2.5 text-sm leading-relaxed",m.from==="user"?"rounded-br-sm border-primary/20 bg-primary text-primary-foreground":"rounded-bl-sm border-border bg-card text-card-foreground")}><p className="whitespace-pre-line">{m.text}</p><span className="mt-1 flex items-center justify-end gap-1 text-[10px] opacity-70">{m.time}{m.from==="user"?<CheckCheck className="size-3"/>:<Check className="size-3"/>}</span></div></div>)}</div>{footerNote&&<p className="border-t border-border bg-card px-4 py-3 text-xs text-muted-foreground">{footerNote}</p>}</div>}
