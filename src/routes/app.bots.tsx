import { createFileRoute, Link } from '@tanstack/react-router';
import { useEffect, useState } from 'react';
import { AppShell } from '@/components/app/AppShell';
import { getSession } from '@/lib/botlab/auth';
import { listBots } from '@/lib/botlab/bots';
import { listWorkspaces } from '@/lib/botlab/workspaces';
export const Route=createFileRoute('/app/bots')({component:BotsPage});
function BotsPage(){const [bots,setBots]=useState<any[]>([]);useEffect(()=>{(async()=>{const s=await getSession();if(!s.data.session){location.href='/auth';return;}const w=await listWorkspaces();if(w[0])setBots(await listBots(w[0].id));})()},[]);return <AppShell><div className="p-5 md:p-8"><div className="flex items-end justify-between"><div><p className="text-sm text-slate-500">Automation</p><h1 className="mt-1 text-3xl font-black">My bots</h1></div><Link to="/app/bots/new" className="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">+ New bot</Link></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{bots.map(b=><div key={b.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-start justify-between"><div className="grid h-11 w-11 place-items-center rounded-xl bg-slate-950 text-white">🤖</div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{b.status}</span></div><h2 className="mt-5 font-bold">{b.name}</h2><p className="mt-1 text-sm text-slate-500">{b.description||'Business AI assistant'}</p><div className="mt-5 flex gap-2 text-xs text-slate-500"><span>{b.language.toUpperCase()}</span><span>·</span><span>{b.channels?.join(', ')}</span></div></div>)}{bots.length===0&&<div className="col-span-full rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center"><p className="font-bold">No bots yet</p><Link className="mt-3 inline-block text-sm font-semibold underline" to="/app/bots/new">Create your first bot</Link></div>}</div></div></AppShell>}
