import{createFileRoute}from'@tanstack/react-router';import{useEffect,useState}from'react';import{AppShell}from'@/components/app/AppShell';import{supabase}from'@/lib/supabase';export const Route=createFileRoute('/app/analytics')({component:Analytics});function Analytics(){const[c,setC]=useState(0),[l,setL]=useState(0),[m,setM]=useState(0),[b,setB]=useState(0);useEffect(()=>{(async()=>{if(!supabase)return;const[cs,ls,ms,bs]=await Promise.all([supabase.from('conversations').select('*',{count:'exact',head:true}),supabase.from('leads').select('*',{count:'exact',head:true}),supabase.from('messages').select('*',{count:'exact',head:true}),supabase.from('bots').select('*',{count:'exact',head:true})]);setC(cs.count||0);setL(ls.count||0);setM(ms.count||0);setB(bs.count||0)})()},[]);return <AppShell><div className="p-5 md:p-8"><h1 className="text-3xl font-black">Analytics</h1><p className="mt-1 text-slate-500">Live product activity across your workspace.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Card n={c}t="Conversations"/><Card n={l}t="Leads"/><Card n={m}t="Messages"/><Card n={b}t="Bots"/></div><div className="mt-6 rounded-2xl border bg-white p-6"><h2 className="font-bold">What to watch</h2><div className="mt-4 grid gap-3 md:grid-cols-3"><Tip t="Lead rate"v={c?`${Math.round(l/c*100)}%`:'0%'}/><Tip t="Messages / conversation"v={c?`${(m/c).toFixed(1)}`:'0'}/><Tip t="Active opportunity"v={`${l} leads`}/></div></div></div></AppShell>}function Card(p:any){return <div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{p.t}</p><p className="mt-2 text-3xl font-black">{p.n}</p></div>}function Tip(p:any){return <div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">{p.t}</p><b className="text-xl">{p.v}</b></div>}
